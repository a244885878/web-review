import{_ as a,c as l,o,ae as i}from"./chunks/framework.Cthp9TdA.js";const D=JSON.parse('{"title":"","description":"","frontmatter":{},"headers":[],"relativePath":"06_http/DNS 记录和报文.md","filePath":"06_http/DNS 记录和报文.md"}'),s={name:"06_http/DNS 记录和报文.md"};function r(t,e,d,n,S,N){return o(),l("div",null,e[0]||(e[0]=[i('<blockquote><p>DNS 记录和报文是域名系统（DNS）中两个密切相关的概念，但它们代表不同的东西。简单来说：</p></blockquote><ul><li>DNS 记录 是存储在 DNS 服务器上的数据条目，包含了域名和 IP 地址之间的映射关系以及其他相关信息。</li><li>DNS 报文 是客户端和 DNS 服务器之间传递的通信消息，用于查询和响应 DNS 记录。</li></ul><h4 id="_1-dns-记录-dns-records" tabindex="-1">1. DNS 记录（DNS Records） <a class="header-anchor" href="#_1-dns-记录-dns-records" aria-label="Permalink to &quot;1. DNS 记录（DNS Records）&quot;">​</a></h4><p>DNS 记录也称为资源记录（Resource Records），它们存储在 DNS 服务器的区域文件中。每条记录都包含有关特定域的信息，例如其 IP 地址、邮件服务器等。常见的 DNS 记录类型包括：</p><ul><li>A 记录（Address Record）：将域名映射到 IPv4 地址。例如，example.com 的 A 记录可能指向 <code>192.0.2.1</code>。</li><li>AAAA 记录（Quad-A Record）：将域名映射到 IPv6 地址。例如，example.com 的 AAAA 记录可能指向 <code>2001:db8::1</code>。</li><li>CNAME 记录（Canonical Name Record）：将一个域名或子域名指向另一个域名（别名）。例如，www.example.com 的 CNAME 记录可能指向 example.com。</li><li>MX 记录（Mail Exchange Record）：指定负责接收域的电子邮件的邮件服务器。</li><li>NS 记录（Name Server Record）：指定负责域的 DNS 解析的权威 DNS 服务器。</li></ul><h4 id="_2-dns-报文-dns-messages" tabindex="-1">2. DNS 报文（DNS Messages） <a class="header-anchor" href="#_2-dns-报文-dns-messages" aria-label="Permalink to &quot;2. DNS 报文（DNS Messages）&quot;">​</a></h4><p>DNS 报文是客户端（例如你的计算机）和 DNS 服务器之间交换的数据包。这些报文用于查询 DNS 记录并将响应返回给客户端。DNS 报文通常使用 UDP 协议进行传输，但也可能使用 TCP 协议。</p><h5 id="dns-报文主要分为两种类型" tabindex="-1">DNS 报文主要分为两种类型： <a class="header-anchor" href="#dns-报文主要分为两种类型" aria-label="Permalink to &quot;DNS 报文主要分为两种类型：&quot;">​</a></h5><ul><li>DNS 查询报文（DNS Query Message）：客户端发送给 DNS 服务器以请求特定域名的信息的报文。查询报文包含要查询的域名和记录类型等信息。</li><li>DNS 响应报文（DNS Response Message）：DNS 服务器响应客户端查询的报文。响应报文包含查询结果，例如域名对应的 IP 地址或其他相关信息。</li></ul><h5 id="dns-报文的结构通常包括以下部分" tabindex="-1">DNS 报文的结构通常包括以下部分： <a class="header-anchor" href="#dns-报文的结构通常包括以下部分" aria-label="Permalink to &quot;DNS 报文的结构通常包括以下部分：&quot;">​</a></h5><ul><li>头部（Header）：包含报文类型、ID、标志等信息。</li><li>问题段（Question Section）：包含查询的问题，例如要查询的域名和记录类型。</li><li>答案段（Answer Section）：包含查询的答案，例如域名对应的 IP 地址。</li><li>授权记录段（Authority Section）：包含授权 DNS 服务器的信息。</li><li>附加信息段（Additional Section）：包含其他附加信息。</li></ul><h4 id="总结" tabindex="-1">总结 <a class="header-anchor" href="#总结" aria-label="Permalink to &quot;总结&quot;">​</a></h4><p>DNS 记录是存储在 DNS 服务器上的数据，而 DNS 报文是客户端和 DNS 服务器之间传递的通信消息。它们共同协作，使得用户可以通过域名访问互联网资源。</p>',13)]))}const _=a(s,[["render",r]]);export{D as __pageData,_ as default};
